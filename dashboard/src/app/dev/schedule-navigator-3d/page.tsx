"use client";

import { ScheduleNavigator3D } from "@/components/schedule-navigator-3d/ScheduleNavigator3D";

/**
 * Temporary review route — not linked from AppShell nav.
 * Visit /dev/schedule-navigator-3d while the 2D navigator remains on /.
 */
export default function ScheduleNavigator3DDevPage() {
  return <ScheduleNavigator3D />;
}
